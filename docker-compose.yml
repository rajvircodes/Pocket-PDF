services:
  api:
    build: ./apps/api
    ports: ["4000:4000"]
    environment:
      PORT: 4000
      UPLOAD_DIR: /tmp/pocket-pdf
    volumes:
      - api_tmp:/tmp/pocket-pdf
  web:
    build: ./apps/web
    ports: ["8080:80"]
    depends_on: [api]
volumes:
  api_tmp:
